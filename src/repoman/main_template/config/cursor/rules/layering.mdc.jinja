---
description: Keep CLI (Typer/Rich) in cli/; core logic in src; no CLI imports in core.
globs: "src/{{ python_package_import_name }}/**/*.py"
alwaysApply: true
---

# Layering

{% if python_package_command_line_name %}
- **Core logic** (`config`{% if fastapi_enabled %}, `app`{% endif %}{% if rag_enabled %}, `rag`{% endif %}{% if dataset_enabled %}, `datasets`{% endif %} and domain modules): Pure logic with no Typer, Rich, or `cli` imports. Inputs and outputs stay data-oriented.
- **cli/**: Commands call into core modules; all Typer/Rich/UX live here.
- **Direction**: `cli` → core. Never the reverse.
{% else %}
- **Core logic** in `src/{{ python_package_import_name }}/`: Keep modules focused; avoid UI/CLI concerns in core.
{% endif %}
