"""LLM port: generate answer from query + context (stub in core)."""

from abc import ABC, abstractmethod


class LLMPort(ABC):
    """Abstract interface for LLM (generate answer from prompt)."""

    @abstractmethod
    def generate(self, prompt: str) -> str:
        """Generate text from prompt.

        Args:
            prompt: Full prompt (query + context).

        Returns:
            Generated answer text.
        """
        ...
{% endif %}
