# API Reference

The public C++ headers are in `include/{{ cpp_namespace }}/`.

Run Doxygen locally when you need generated API artifacts:

```bash
make docs-api
```

The hand-written API overview should stay in this page or in nearby reference
pages. Generated Doxygen output is kept under `build/doxygen/`.
