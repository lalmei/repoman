#include "{{ cpp_namespace }}/{{ cpp_namespace }}.hpp"

#include <cassert>
#include <string>

int main() {
  assert({{ cpp_namespace }}::add(2, 3) == 5);
  assert(!{{ cpp_namespace }}::version().empty());
  return 0;
}
